<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Climate Projects</title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body class="bg-gray-100">
    <%- include('partials/navbar', { page: '/solutions/projects' }) %>

    <div class="container mx-auto p-6">
        <section class="hero bg-blue-500 text-white text-center p-10 rounded-lg">
            <div class="hero-content text-center">
                <div class="max-w-md">
                <h1 class="text-3xl font-bold leading-snug">Projects</h1>
                <p class="mt-4">Browse the current list of Projects in our database. Click the theme to filter by sectors such as:</p>
                <a href="/solutions/projects?sector=electricity" class="btn bg-white text-blue-500 mt-4 inline-block">Electricity</a>
            </div>
        </section>
        <!-- Table -->
        <div class="overflow-x-auto">
            <table class="table w-full max-w-4xl mx-auto table-auto border-collapse bg-white shadow-md rounded-lg">
                <thead>
                    <tr class="bg-gray-800 text-white">
                        <th class="px-4 py-3 font-medium">Title</th>
                        <th class="px-4 py-3 font-medium">Sector</th>
                        <th class="px-4 py-3 font-medium">Summary</th>
                        <th class="px-4 py-3 font-medium">Details</th>
                    </tr>
                </thead>
                <tbody>
                    <% projects.forEach(project => { %>
                    <tr class="hover:bg-gray-50">
                        <td class="px-4 py-3 text-gray-800"><%= project.title %></td>
                        <td class="px-4 py-3">
                            <a href="/solutions/projects?sector=<%= project.sector %>" class="btn btn-sm btn-primary text-white">
                                <%= project.sector %>
                            </a>
                        </td>
                        <td class="px-4 py-3 text-gray-600"><%= project.summary_short %></td>
                        <td class="px-4 py-3">
                            <a href="/solutions/projects/<%= project.id %>" class="btn btn-sm btn-info text-white">
                                Details
                            </a>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>
